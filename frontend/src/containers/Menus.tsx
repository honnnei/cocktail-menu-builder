import React from 'react';
import Carousel from '../components/Carousel';

const Menus = () => {
    return (
        <div>
            <h1>Menus</h1>
            <Carousel />
        </div>
    );
}

export default Menus;
